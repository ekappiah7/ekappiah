{
  "name": "family-ledger-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project family-ledger-rules-test \"node --test tests/rules.test.mjs\"",
    "test:e2e": "firebase emulators:exec --only firestore,auth --project family-ledger-rules-test \"node tests/sync.e2e.mjs\"",
    "serve": "python3 -m http.server 8777 --directory .."
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.29.1",
    "playwright": "^1.49.1"
  }
}
